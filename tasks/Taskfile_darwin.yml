version: "3"

includes:
  internal: ./Taskfile_internal.yml

tasks:
  int-build:
    summary: Internal build task for macOS
    platforms: [darwin]
    internal: true
    cmds:
      - cmd: echo BUILDING FOR macOS $GOARCH
      - task: build

  build:
    summary: Builds MassaStation for macOS
    cmds:
      - task: internal:build
        vars:
          OS: darwin
          ARCH: '{{.ARCH | default ""}}'

  build-prod:
    summary: Builds MassaStation for macOS (production)
    cmds:
      - task: internal:build-prod
        vars:
          OS: darwin
          ARCH: '{{.ARCH | default ""}}'
